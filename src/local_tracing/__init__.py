"""Lightweight local tracing utilities for one-shot-bench.

This package provides:
- mitm_tracer: mitmproxy addon that records raw HTTP traces to a SQLite DB
- trace_cleaner: simple worker that copies raw traces into a clean DB
"""


