[pytest]
addopts = --ignore=data/tasks --ignore=scripts/create_tasks --timeout=300
markers =
    integration: integration tests that hit external services
    slow: slow tests that take a long time to run
